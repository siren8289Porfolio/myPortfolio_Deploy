# Production / EC2 — GHCR pre-built images (no build on EC2)
services:
  nginx:
    image: nginx:latest
    container_name: portfolio-nginx
    ports:
      - "${NGINX_HTTP_PORT:-80}:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - mido-app
      - legacy-app
      - pivot-app
      - allohub-app
      - if-app
      - golmok-app
      - briefly-app
    networks:
      - portfolio-network
    restart: unless-stopped

  mido-app:
    image: ghcr.io/${GHCR_OWNER:-siren8289porfolio}/portfolio-mido:${GHCR_IMAGE_TAG:-latest}
    container_name: mido-app
    expose:
      - "8080"
    environment:
      SERVER_SERVLET_CONTEXT_PATH: /mido
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-local}
      TZ: ${TZ:-Asia/Seoul}
    networks:
      - portfolio-network
    restart: unless-stopped

  legacy-app:
    image: ghcr.io/${GHCR_OWNER:-siren8289porfolio}/portfolio-legacy:${GHCR_IMAGE_TAG:-latest}
    container_name: legacy-app
    expose:
      - "8080"
    environment:
      SERVER_SERVLET_CONTEXT_PATH: /legacy
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-local}
      TZ: ${TZ:-Asia/Seoul}
    networks:
      - portfolio-network
    restart: unless-stopped

  pivot-app:
    image: ghcr.io/${GHCR_OWNER:-siren8289porfolio}/portfolio-pivot:${GHCR_IMAGE_TAG:-latest}
    container_name: pivot-app
    expose:
      - "8080"
    environment:
      SERVER_SERVLET_CONTEXT_PATH: /pivot
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-local}
      TZ: ${TZ:-Asia/Seoul}
    networks:
      - portfolio-network
    restart: unless-stopped

  allohub-app:
    image: ghcr.io/${GHCR_OWNER:-siren8289porfolio}/portfolio-allohub:${GHCR_IMAGE_TAG:-latest}
    container_name: allohub-app
    expose:
      - "8080"
    environment:
      SERVER_SERVLET_CONTEXT_PATH: /allohub
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-local}
      TZ: ${TZ:-Asia/Seoul}
    networks:
      - portfolio-network
    restart: unless-stopped

  if-app:
    image: ghcr.io/${GHCR_OWNER:-siren8289porfolio}/portfolio-if:${GHCR_IMAGE_TAG:-latest}
    container_name: if-app
    expose:
      - "8080"
    environment:
      SERVER_SERVLET_CONTEXT_PATH: /if
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-local}
      TZ: ${TZ:-Asia/Seoul}
    networks:
      - portfolio-network
    restart: unless-stopped

  golmok-app:
    image: ghcr.io/${GHCR_OWNER:-siren8289porfolio}/portfolio-golmok:${GHCR_IMAGE_TAG:-latest}
    container_name: golmok-app
    expose:
      - "8080"
    environment:
      SERVER_SERVLET_CONTEXT_PATH: /golmok
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILE:-local}
      TZ: ${TZ:-Asia/Seoul}
    networks:
      - portfolio-network
    restart: unless-stopped

  briefly-app:
    image: ghcr.io/${GHCR_OWNER:-siren8289porfolio}/portfolio-briefly:${GHCR_IMAGE_TAG:-latest}
    container_name: briefly-app
    expose:
      - "8080"
    environment:
      TZ: ${TZ:-Asia/Seoul}
    networks:
      - portfolio-network
    restart: unless-stopped

networks:
  portfolio-network:
    name: portfolio-network
    driver: bridge
