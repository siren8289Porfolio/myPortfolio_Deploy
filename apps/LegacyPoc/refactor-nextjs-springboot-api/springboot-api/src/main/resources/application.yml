server:
  port: 8083

app:
  repository:
    type: memory

spring:
  application:
    name: springboot-api-nextjs
  datasource:
    url: jdbc:postgresql://localhost:5432/legacy_next_refactor
    username: legacy_user
    password: legacy_pass
    driver-class-name: org.postgresql.Driver
