server:
  port: 8081

app:
  repository:
    type: memory

spring:
  application:
    name: springboot-refactor
  datasource:
    url: jdbc:mariadb://localhost:3306/legacy_refactor
    username: legacy_user
    password: legacy_pass
    driver-class-name: org.mariadb.jdbc.Driver
