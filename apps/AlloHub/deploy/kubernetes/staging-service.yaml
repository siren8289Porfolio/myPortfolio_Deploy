apiVersion: v1
kind: Service
metadata:
  name: allochub-api
  namespace: staging
spec:
  selector:
    app: allochub-api
  ports:
    - port: 443
      targetPort: 3000
      protocol: TCP
  type: LoadBalancer
