server:
  port: ${PORT:8080}
  servlet:
    context-path: ${SERVER_SERVLET_CONTEXT_PATH:}

spring:
  application:
    name: allochub
  jpa:
    open-in-view: false

allochub:
  auth:
    operator-token: ${ALLOC_OPERATOR_TOKEN:operator-dev-token}
    admin-token: ${ALLOC_ADMIN_TOKEN:admin-dev-token}

management:
  endpoints:
    web:
      exposure:
        include: health,info
